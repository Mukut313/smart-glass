# Smart Glass for Blind People — Raspberry Pi 5
# Install inside a venv: pip install -r requirements.txt

# Core vision & ML
easyocr>=1.7.1
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
torch>=2.1.0
torchvision>=0.16.0

# Hybrid OCR refinement: PaddleOCR re-recognises Latin/English-script lines
# for higher accuracy (EasyOCR remains the only engine with Bangla support,
# so it stays primary — see modes/ocr_mode.py for the full rationale).
# NOTE: prebuilt ARM64 (RPi 5) wheels for paddlepaddle are not always
# available on PyPI for every Raspberry Pi OS / Python version. The app
# degrades gracefully to EasyOCR-only if this import fails, so treat this
# as a "best effort" install — see install.sh step 3 for the ARM64 notes.
paddleocr>=2.7.0
paddlepaddle>=2.5.0

# GPIO — REQUIRED for RPi 5 (replaces incompatible RPi.GPIO)
rpi-lgpio>=0.5

# Utilities
numpy>=1.24.0
Pillow>=10.0.0

# Audio / TTS playback
pygame>=2.5.0
gTTS>=2.5.0

# Optional: offline voice commands (English only — no Bangla Vosk model exists)
# vosk>=0.3.45
# pyaudio>=0.2.13
